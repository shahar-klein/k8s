apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
  name: sriov-network2
spec:
  config: '{
    "type": "sriov",
    "if0": "enp12s0",
    "ipam": {
            "type": "dhcp"
    }
  }'
