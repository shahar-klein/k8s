apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
  name: sriov-network3
spec:
  config: '{
    "type": "sriov",
    "if0": "enp132s0",
    "ipam": {
            "type": "dhcp"
    }
  }'
